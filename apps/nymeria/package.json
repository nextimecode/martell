{
  "name": "nymeria",
  "version": "0.0.0-development",
  "description": "Parte logada do app - Nymeria, inspirada em Nymeria Sand, reflete astúcia, elegância e determinação. Este projeto simboliza a precisão e a sofisticação da área logada, desenvolvida para proporcionar uma experiência fluida, funcional e com design moderno, garantindo qualidade e eficiência à altura das mais altas expectativas.",
  "private": true,
  "scripts": {
    "dev": "rm -rf .next && next dev --port 3001",
    "clean": "rm -rf .turbo .next dist node_modules pnpm-lock.yaml",
    "build": "next build",
    "start": "next start --port 3001",
    "lint": "next lint --fix",
    "e2e": "pnpm exec playwright test",
    "e2e:ui": "pnpm exec playwright test --ui",
    "e2e:chromium": "pnpm exec playwright test --project=chromium",
    "e2e:debug": "pnpm exec playwright test --debug",
    "e2e:codegen": "pnpm exec playwright codegen",
    "e2e:report": "pnpm exec playwright show-report",
    "typecheck": "tsc --noEmit"
  }
}
